GEMINI_API_KEY=your-gemini-api-key-here

# Jenkins Debugger (Sprint 1)
# For local dev with mock server: leave as-is (mock accepts any non-empty user/token)
# For real Jenkins: set JENKINS_URL to your Jenkins base URL
JENKINS_USER=dev
JENKINS_TOKEN=dev-token
